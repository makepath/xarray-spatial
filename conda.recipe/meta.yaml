{% set sdata = load_setup_py_data() %}

package:
  name: xarray-spatial
  version: 0.1.2

source:
  path: ..
  
requirements:
  host:
    - python
    - setuptools
    - pyct
  run:
    - python
    {% for dep in sdata.get('install_requires', {}) %}
    - {{ dep }}
    {% endfor %}
    
# will need extras_require['tests'] once those are added
test:
  requires:
    - pytest >=2.8.5
    # {% for dep in sdata['extras_require']['tests'] %}
    # - {{ dep }}
    # {% endfor %}
  imports:
    - xrspatial

about:
  home: https://github.com/makepath/xarray-spatial
  license: MIT
