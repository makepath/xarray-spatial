name: flake8 Lint

on: [push, pull_request]

jobs:
  flake8-lint:
    runs-on: ubuntu-latest
    name: Lint with flake8
    steps:
      - uses: actions/checkout@v1
      - id: file_changes
        uses: trilom/file-changes-action@v1.2.3
      - name: Get modified files
        run: |
          echo '${{ steps.file_changes.outputs.files_modified}}' | sed -e 's/\[//g; s/\]//g; s/\,/ /g; s/\"//g' -e 's/[0-9]/N/g' > files.txt
      - uses: ricardochaves/python-lint@v1.3.0
        with:
          python-root-list: cat files.txt
          use-pylint: false
          use-pycodestyle: false
          use-flake8: true
          use-black: false
          use-mypy: false
          use-isort: false