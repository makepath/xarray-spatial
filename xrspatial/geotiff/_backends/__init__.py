"""Per-backend implementations for the geotiff read entry points.

Holds ``_gpu_helpers.py``, ``gpu.py``, ``dask.py``, and ``vrt.py`` for
the backend entry-point bodies. The package ``__init__`` stays empty
so nothing leaks into ``xrspatial.geotiff`` through implicit
re-exports.
"""
